<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="colors.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:colors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="colors">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>
    <Grid Background="{StaticResource AppBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- TitleBar -->
            <RowDefinition Height="*"/>    <!-- Content -->
            <RowDefinition Height="Auto"/> <!-- Bottom Nav -->
        </Grid.RowDefinitions>

        <!-- Custom Title Bar -->
        <Grid x:Name="AppTitleBar" Grid.Row="0" Height="40" Background="Transparent">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" Margin="16,0" Spacing="8" VerticalAlignment="Center">
                <FontIcon Glyph="&#xE790;" FontSize="14" Foreground="{StaticResource TextFillColorSecondaryBrush}"/>
                <TextBlock Text="Colors" FontSize="14" Foreground="{StaticResource TextFillColorSecondaryBrush}"/>
            </StackPanel>
        </Grid>

        <!-- Content Frame -->
        <Frame x:Name="ContentFrame" Grid.Row="1" Background="Transparent"/>

        <!-- Bottom Navigation Bar -->
        <Grid Grid.Row="2" Background="{StaticResource CardBackgroundBrush}" BorderBrush="{StaticResource CardStrokeColorDefaultBrush}" BorderThickness="0,1,0,0" Padding="0,8,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Projects Tab -->
            <Button x:Name="ProjectsButton" Grid.Column="0" Click="ProjectsButton_Click" Background="Transparent" BorderThickness="0" HorizontalAlignment="Stretch" HorizontalContentAlignment="Center">
                <StackPanel Spacing="4">
                    <FontIcon Glyph="&#xE82D;" FontSize="20" Foreground="{StaticResource TextFillColorPrimaryBrush}"/>
                    <TextBlock Text="Projects" FontSize="10" Foreground="{StaticResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center"/>
                </StackPanel>
            </Button>

            <!-- Picker Tab -->
            <Button x:Name="ScreenPickerButton" Grid.Column="1" Click="ScreenPickerButton_Click" Background="Transparent" BorderThickness="0" HorizontalAlignment="Stretch" HorizontalContentAlignment="Center">
                <StackPanel Spacing="4">
                    <FontIcon Glyph="&#xEF3C;" FontSize="20" Foreground="{StaticResource TextFillColorPrimaryBrush}"/>
                    <TextBlock Text="Pick" FontSize="10" Foreground="{StaticResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center"/>
                </StackPanel>
            </Button>

            <!-- Image Tab -->
            <Button x:Name="ImagePaletteButton" Grid.Column="2" Click="ImagePaletteButton_Click" Background="Transparent" BorderThickness="0" HorizontalAlignment="Stretch" HorizontalContentAlignment="Center">
                <StackPanel Spacing="4">
                    <FontIcon Glyph="&#xE8B9;" FontSize="20" Foreground="{StaticResource TextFillColorPrimaryBrush}"/>
                    <TextBlock Text="Image" FontSize="10" Foreground="{StaticResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>
    </Grid>
</Window>
