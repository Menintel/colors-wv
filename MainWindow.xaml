<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="colors.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:colors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="colors">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->

        <Grid x:Name="AppTitleBar" Grid.Row="0" Background="{ThemeResource  CardBackgroundFillColorDefaultBrush}" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="188"/> <!-- Space for Caption Buttons -->
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" Margin="16,0" Spacing="8">
                <FontIcon Glyph="&#xE790;"/>
                <TextBlock
                Text="Color Picker"
                VerticalAlignment="Center"
                FontSize="16"
                FontWeight="SemiBold"/>
            </StackPanel>

            <!-- Navigation Buttons -->
            <StackPanel
                Grid.Column="1"
                Orientation="Horizontal"
                Spacing="8"
                Margin="0,0,16,0">
                
                <Button
                    x:Name="ProjectsButton"
                    Click="ProjectsButton_Click"
                    Style="{StaticResource AccentButtonStyle}"
                    ToolTipService.ToolTip="Projects">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE82D;"/>
                        <TextBlock Text="Projects"/>
                    </StackPanel>
                </Button>
                
                <Button
                    x:Name="ScreenPickerButton"
                    Click="ScreenPickerButton_Click"
                    ToolTipService.ToolTip="Screen Picker">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xEF3C;"/>
                        <TextBlock Text="Pick"/>
                    </StackPanel>
                </Button>
                
                <Button
                    x:Name="ImagePaletteButton"
                    Click="ImagePaletteButton_Click"
                    ToolTipService.ToolTip="New from Image">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE8B9;"/>
                        <TextBlock Text="Image"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Content Frame -->
        <Frame x:Name="ContentFrame" Grid.Row="1"/>
    </Grid>
</Window>
