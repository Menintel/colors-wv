<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="colors.Views.PalettesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:colors.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{StaticResource AppBackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Padding="16" Background="Transparent">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Spacing="2">
                <TextBlock Text="Palettes" FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource TextFillColorPrimaryBrush}"/>
                <TextBlock Text="All your collections" FontSize="12" Foreground="{StaticResource TextFillColorSecondaryBrush}"/>
            </StackPanel>

            <Button 
                Grid.Column="1"
                Click="RefreshButton_Click"
                Style="{StaticResource SubtleButtonStyle}"
                Padding="8">
                <FontIcon Glyph="&#xE72C;" FontSize="14"/>
            </Button>
        </Grid>

        <!-- Palettes List -->
        <ListView 
            x:Name="PalettesList" 
            Grid.Row="1" 
            Padding="16,0,16,16" 
            SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,0,0,16">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Header: Name + Menu -->
                        <Grid Grid.Row="0" Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock 
                                Text="{Binding Name}" 
                                FontSize="14" 
                                FontWeight="SemiBold" 
                                Foreground="{StaticResource TextFillColorPrimaryBrush}"/>

                            <Button 
                                Grid.Column="1"
                                Content="&#xE712;" 
                                FontFamily="Segoe MDL2 Assets"
                                Background="Transparent" 
                                BorderThickness="0"
                                FontSize="12"
                                Padding="4"
                                MinHeight="24"
                                Flyout="{StaticResource PaletteMenuFlyout}"/>
                        </Grid>

                        <!-- Colors Row -->
                        <ItemsControl Grid.Row="1" ItemsSource="{Binding ColorsPreview}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" Spacing="0"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border 
                                        Width="32" 
                                        Height="32" 
                                        Background="{Binding WindowsColor}"
                                        ToolTipService.ToolTip="{Binding HexColor}">
                                        <!-- First item radius logic would be complex in XAML only, keeping simple rectangles for now or using CornerRadius on the container if needed -->
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0"/>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

        <!-- Loading / Empty States -->
        <ProgressRing x:Name="LoadingRing" Grid.Row="1" IsActive="False" Visibility="Collapsed" Width="40" Height="40"/>
        
        <StackPanel x:Name="EmptyState" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Collapsed" Spacing="8">
            <TextBlock Text="No palettes found" Foreground="{StaticResource TextFillColorSecondaryBrush}"/>
            <TextBlock Text="Create one from the Image tab" Foreground="{StaticResource TextFillColorTertiaryBrush}" FontSize="12"/>
        </StackPanel>

    </Grid>

    <Page.Resources>
        <MenuFlyout x:Key="PaletteMenuFlyout">
            <MenuFlyoutItem Text="Delete" Click="DeletePalette_Click" Tag="{Binding}"/>
            <MenuFlyoutItem Text="Copy JSON" Click="CopyJson_Click" Tag="{Binding}"/>
        </MenuFlyout>
    </Page.Resources>
</Page>
