<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="colors.Views.ImagePalettePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:colors.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Spacing="16">
            <TextBlock Text="Create Palette from Image" FontSize="24" FontWeight="SemiBold"/>
            <Button x:Name="UploadImageButton" Content="Upload Image" Click="UploadImageButton_Click"/>
        </StackPanel>

        <!-- Content -->
        <StackPanel Grid.Row="1" Margin="0,24,0,0" Spacing="16">
            <TextBlock x:Name="StatusText" Text="Upload an image to extract colors" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </StackPanel>
    </Grid>
</Page>
